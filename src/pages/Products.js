import AllMeals from '../components/Layout/Meals/AllMeals'

export const Menu = (props) => {
    return (
        <section style={{ paddingTop: 50 }}>
            <AllMeals />
        </section>
    )
}

export default Menu